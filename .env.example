# Claude Code Subscription Tier
# Options: free, pro, max_5x, max_20x, auto
# If set to "auto" or left empty, will attempt to detect from ~/.claude/.credentials.json
CLAUDE_TIER=pro

# Disable colored output (set to 1 or true to disable)
# NO_COLOR=1

# Progress bar width (default: 42, range: 20-100)
# PROGRESS_WIDTH=42
